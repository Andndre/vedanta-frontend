<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BrahmacarLearning"
        tools:targetApi="31">
        <activity
            android:name=".view.user.detailchat.DetailChatActivity"
            android:exported="false" />
        <activity
            android:name=".view.user.detail.DetailActivity"
            android:exported="false" />
        <activity
            android:name=".view.user.chat.ChatActivity"
            android:exported="false" />
        <activity
            android:name=".view.role.RoleActivity"
            android:exported="false" />
        <activity
            android:name=".view.user.main.MainActivity"
            android:exported="false" />
        <activity
            android:name=".view.user.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".view.user.register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".view.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>